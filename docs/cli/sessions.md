---
summary: "CLI reference for `starforge sessions` (list stored sessions + usage)"
read_when:
  - You want to list stored sessions and see recent activity
title: "sessions"
---

# `starforge sessions`

List stored conversation sessions.

```bash
starforge sessions
starforge sessions --active 120
starforge sessions --json
```
