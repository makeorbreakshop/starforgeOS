---
summary: "CLI reference for `starforge uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
title: "uninstall"
---

# `starforge uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
starforge uninstall
starforge uninstall --all --yes
starforge uninstall --dry-run
```
